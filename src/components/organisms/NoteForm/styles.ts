import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  submitButton: {
    backgroundColor: '#4CAF50',
    padding: 24,
    borderRadius: 32,
    alignItems: 'center',
    marginTop: 24,
  },
  submitButtonText: {
    color: '#FFFFFF',
    fontWeight: 'bold',
    fontSize: 16,
  },
});
